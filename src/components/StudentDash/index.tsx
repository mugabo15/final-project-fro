import { useState } from 'react';
import DocumentsPage from './dash';

const StudentDashboard = () => {
 
  return (
    <div>
      <DocumentsPage />
    </div>
  );
};

export default StudentDashboard;